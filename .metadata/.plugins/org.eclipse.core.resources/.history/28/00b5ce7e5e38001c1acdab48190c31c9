# 指定运行端口
server:
  port: 8081

# 指定主机地址
eureka:
  instance:
    hostname: server2
  client:
    fetch-registry: true
    register-with-eureka: true
    service-url:
      # 注册到另一个Eureka注册中心
      defaultZone: http://127.0.0.1:8002/eureka/

